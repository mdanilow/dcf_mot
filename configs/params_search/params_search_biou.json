{
    "tracker_config": {
        "det_conf_thresholds": "[(a, 0.7) for a in np.linspace(0.1, 0.25, 4)]",
        "match_thresholds": "[(a, b) for a in np.linspace(0.1, 0.9, 10) for b in np.linspace(0.1, 0.9, 10) if a > b]",
        "biou_buffer_sizes": "[(a, b) for a in np.linspace(0.1, 0.6, 6) for b in np.linspace(0.2, 0.7, 6) if a < b]"
    }
}